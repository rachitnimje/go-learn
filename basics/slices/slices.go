package main

import "fmt"

func main() {
	// slices: flexible wrapper on top of an array.
	// they don't own any data and just are references to an existing array

	fmt.Println("\n-----------------------DECLARATIONS---------------------------")
	declarations()

	fmt.Println("\n------------------------PROPERTIES----------------------------")
	properties()
}
